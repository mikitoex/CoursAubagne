backend:
  name: git-gateway
  branch: main

# AJOUTE LE SLASH / AU DÉBUT ICI 👇
media_folder: "/uploads"
public_folder: "/uploads"

collections:
  - name: "quiz"
    label: "Quiz & Catégories"
    files:
      - name: "data"
        label: "Mes Quiz"
        file: "quiz.json"
        fields:
          - {label: "Liste des Catégories", name: "categories", widget: "list", fields: [
              {label: "Identifiant (ex: jeune)", name: "key", widget: "string"},
              {label: "Titre Affiché", name: "title", widget: "string"},
              {label: "Emoji", name: "icon", widget: "string"},
              {label: "PDF à gagner", name: "pdf", widget: "file", required: false},
              {label: "Questions", name: "questions", widget: "list", fields: [
                  {label: "Question", name: "question", widget: "string"},
                  {label: "Réponses (séparez par virgule)", name: "options", widget: "list"},
                  {label: "Bonne réponse (copie exacte)", name: "answer", widget: "string"},
                  {label: "Explication", name: "explanation", widget: "text"}
              ]}
            ]}
