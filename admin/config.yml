backend:
  name: git-gateway
  branch: main

media_folder: "/uploads"
public_folder: "/uploads"
display_url: "https://aubagnecours.netlify.app"
locale: "fr" 

collections:
  - name: "quiz"
    label: "📚 GESTION DES COURS"
    files:
      - name: "data"
        label: "Modifier le contenu"
        file: "quiz.json"
        fields:
          - {
              label: "MES COURS (Clique sur + pour ajouter)", 
              name: "categories", 
              widget: "list", 
              collapsed: true,
              summary: "📂 {{title}}", 
              fields: [
                # --- NOUVEAU : TON PROMPT EST ICI ---
                {
                  label: "🤖 PROMPT IA (Copie ce texte pour ChatGPT)", 
                  name: "ai_helper", 
                  widget: "text", 
                  required: false,
                  hint: "Clique dans la case noire, fais CTRL+A (Tout sélectionner) puis CTRL+C (Copier).",
                  default: "Ton objectif : Créer un quiz de révision simple et clair pour des élèves, basé UNIQUEMENT sur le fichier que je te joins.\n\nRÈGLES DE STYLE (TRES IMPORTANT) :\n1. Parle français naturellement. N'utilise JAMAIS d'expressions comme 'Selon le document', 'Le texte mentionne'. Pose la question directement.\n2. Fais des questions courtes et faciles à lire.\n3. Les options de réponse doivent être simples. Pas de phrases compliquées.\n4. L'explication doit être courte (1 phrase) et instructive.\n\nFORMAT TECHNIQUE :\nDonne-moi UNIQUEMENT le code JSON brut (sans phrase d'intro, sans balises markdown).\nStructure obligatoire :\n[{\"question\": \"Question simple ?\", \"options\": [\"Choix A\", \"Choix B\", \"Choix C\"], \"answer\": \"Choix A\", \"explanation\": \"C'est la bonne réponse car...\"}]\n\nGénère 10 questions maintenant."
                },
                
                # --- LA SUITE NORMALE ---
                {label: "1. Titre du Cours", name: "title", widget: "string"},
                {label: "2. Identifiant (minuscule sans espace)", name: "key", widget: "string"},
                {label: "3. Image (Optionnel)", name: "image", widget: "image", required: false},
                {label: "4. Emoji (Optionnel)", name: "emoji", widget: "string", required: false},
                {label: "5. PDF à gagner (Optionnel)", name: "pdf", widget: "file", required: false},

                # --- ZONE DE COLLAGE ---
                {
                  label: "⚡ ZONE DE COLLAGE (Colle la réponse de l'IA ici)", 
                  name: "bulk_json", 
                  widget: "text", 
                  required: false,
                  hint: "Colle ici le code JSON que l'IA t'a donné."
                },

                # --- ZONE MANUELLE ---
                {
                  label: "✍️ Modification Manuelle", 
                  name: "questions", 
                  widget: "list",
                  collapsed: true, 
                  summary: "{{question}}",
                  fields: [
                    {label: "Question", name: "question", widget: "string"},
                    {label: "Réponses", name: "options", widget: "list"},
                    {label: "Bonne Réponse", name: "answer", widget: "string"},
                    {label: "Explication", name: "explanation", widget: "text", required: false}
                  ]
                }
              ]
            }
