backend:
  name: git-gateway
  branch: main

media_folder: "/uploads"
public_folder: "/uploads"
display_url: "https://aubagnecours.netlify.app"
locale: "fr" 

collections:
  # --- COLLECTION 1 : LE MENU POUR COPIER LE PROMPT (À GAUCHE) ---
  - name: "helper"
    label: "🤖 LE PROMPT (Cliquer ici)"
    files:
      - name: "prompt"
        label: "Copie ce texte"
        file: "prompt_ia.txt" # Un fichier texte simple
        fields:
          - {
              label: "LE TEXTE À COPIER (CTRL+A puis CTRL+C)", 
              name: "body", 
              widget: "text", 
              # TON TEXTE EXACT EST ICI 👇
              default: "Ton objectif : Créer un quiz de révision simple et clair pour des élèves, basé UNIQUEMENT sur le fichier que je te joins.\nRÈGLES DE STYLE (TRES IMPORTANT) :1. Parle français naturellement. N'utilise JAMAIS d'expressions comme 'Selon le document', 'Le texte mentionne', 'D'après l'auteur'. Pose la question directement.2. Fais des questions courtes et faciles à lire.3. Les options de réponse doivent être simples. Pas de phrases compliquées.4. L'explication doit être courte (1 phrase) et instructive.\nFORMAT TECHNIQUE :Donne-moi UNIQUEMENT le code JSON brut (sans phrase d'intro, sans balises markdown).Structure obligatoire :[{\"question\": \"Question simple ?\", \"options\": [\"Choix A\", \"Choix B\", \"Choix C\"], \"answer\": \"Choix A\", \"explanation\": \"C'est la bonne réponse car...\"}]\nGénère 10 questions maintenant."
            }

  # --- COLLECTION 2 : TES COURS (NETTOYÉ) ---
  - name: "quiz"
    label: "📚 GESTION DES COURS"
    files:
      - name: "data"
        label: "Modifier le contenu"
        file: "quiz.json"
        fields:
          - {
              label: "MES COURS (Clique sur + pour ajouter)", 
              name: "categories", 
              widget: "list", 
              collapsed: true,
              summary: "📂 {{title}}", 
              fields: [
                # J'ai enlevé le prompt d'ici pour que ce soit propre !
                
                {label: "1. Titre du Cours", name: "title", widget: "string"},
                {label: "2. Identifiant (minuscule sans espace)", name: "key", widget: "string"},
                {label: "3. Image (Optionnel)", name: "image", widget: "image", required: false},
                {label: "4. Emoji (Optionnel)", name: "emoji", widget: "string", required: false},
                {label: "5. PDF à gagner (Optionnel)", name: "pdf", widget: "file", required: false},

                # ZONE DE COLLAGE
                {
                  label: "⚡ ZONE DE COLLAGE (Colle la réponse de l'IA ici)", 
                  name: "bulk_json", 
                  widget: "text", 
                  required: false,
                  hint: "Va chercher le prompt dans le menu à gauche, donne-le à ChatGPT, et colle sa réponse ici."
                },

                # ZONE MANUELLE
                {
                  label: "✍️ Modification Manuelle", 
                  name: "questions", 
                  widget: "list",
                  collapsed: true, 
                  summary: "{{question}}",
                  fields: [
                    {label: "Question", name: "question", widget: "string"},
                    {label: "Réponses", name: "options", widget: "list"},
                    {label: "Bonne Réponse", name: "answer", widget: "string"},
                    {label: "Explication", name: "explanation", widget: "text", required: false}
                  ]
                }
              ]
            }
