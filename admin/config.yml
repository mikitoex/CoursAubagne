backend:
  name: git-gateway
  branch: main

# Dossier où les images et PDF seront stockés
media_folder: "/uploads"
public_folder: "/uploads"

# Configuration de l'affichage
display_url: "https://aubagnecours.netlify.app"
locale: "fr" 

collections:
  - name: "quiz"
    label: "📚 Mes Catégories de Quiz"
    files:
      - name: "data"
        label: "Gérer les Quiz"
        file: "quiz.json"
        fields:
          - {
              label: "Liste des Catégories", 
              name: "categories", 
              widget: "list", 
              collapsed: false,
              summary: "📂 {{title}} ({{questions.length}} questions)",
              fields: [
                # 1. IDENTIFIANT
                {
                  label: "Identifiant Unique (ID)", 
                  name: "key", 
                  widget: "string",
                  hint: "⚠️ Important : Un mot simple minuscule sans espace (ex: priere)."
                },
                
                # 2. TITRE
                {
                  label: "Titre du Quiz", 
                  name: "title", 
                  widget: "string"
                },
                
                # 3. VISUEL (EMOJI OU IMAGE)
                {
                  label: "Option 1 : Emoji", 
                  name: "emoji", 
                  widget: "string", 
                  required: false,
                  hint: "Écris l'emoji ici (ex: 🕌, 💩) si tu ne mets pas d'image."
                },
                {
                  label: "Option 2 : Image", 
                  name: "image", 
                  widget: "image", 
                  required: false,
                  hint: "Uploade une image ici (Elle sera prioritaire sur l'emoji)."
                },
                
                # 4. LE PDF
                {
                  label: "🎁 Cadeau PDF", 
                  name: "pdf", 
                  widget: "file", 
                  required: false,
                  hint: "Le fichier à gagner si l'élève a 100% de bonnes réponses."
                },

                # 5. MODE TURBO (LA NOUVEAUTÉ)
                {
                  label: "⚡ MODE TURBO (Copier-Coller IA)", 
                  name: "bulk_json", 
                  widget: "text", 
                  required: false,
                  hint: "Colle ici le bloc JSON que ChatGPT te donne. Si tu remplis ça, ne touche pas à la liste 'Questions' ci-dessous."
                },
                
                # 6. LES QUESTIONS (MODE MANUEL)
                {
                  label: "❓ Les Questions (Mode Manuel)", 
                  name: "questions", 
                  widget: "list",
                  collapsed: true, 
                  summary: "{{question}}",
                  fields: [
                    {label: "La Question", name: "question", widget: "string"},
                    {label: "Les Choix (2 à 4)", name: "options", widget: "list"},
                    {label: "La BONNE Réponse", name: "answer", widget: "string", hint: "Copie exactement la bonne réponse."},
                    {label: "Explication", name: "explanation", widget: "text", required: false}
                  ]
                }
              ]
            }
