backend:
  name: git-gateway
  branch: main

# Dossier de stockage
media_folder: "/uploads"
public_folder: "/uploads"

# Langue et affichage
display_url: "https://aubagnecours.netlify.app"
locale: "fr" 

collections:
  - name: "quiz"
    label: "📚 GESTION DES COURS"
    files:
      - name: "data"
        label: "Modifier le contenu"
        file: "quiz.json"
        fields:
          - {
              label: "LISTE DES COURS", 
              name: "categories", 
              widget: "list", 
              collapsed: false,
              summary: "📂 {{title}}",
              fields: [
                # ==========================================
                # ÉTAPE 1 : INFORMATIONS DE BASE
                # ==========================================
                {
                  label: "1️⃣ TITRE DU COURS (Ex: La Prière)", 
                  name: "title", 
                  widget: "string"
                },
                {
                  label: "🔑 IDENTIFIANT UNIQUE (Ex: priere)", 
                  name: "key", 
                  widget: "string",
                  hint: "⚠️ Un seul mot, tout en minuscule, sans accent, sans espace."
                },

                # ==========================================
                # ÉTAPE 2 : APPARENCE (IMAGE OU EMOJI)
                # ==========================================
                {
                  label: "🖼️ IMAGE (Prioritaire)", 
                  name: "image", 
                  widget: "image", 
                  required: false,
                  hint: "Clique pour choisir une image. Si tu mets une image, l'emoji sera ignoré."
                },
                {
                  label: "🙂 EMOJI (Si pas d'image)", 
                  name: "emoji", 
                  widget: "string", 
                  required: false,
                  hint: "Colle un emoji ici (ex: 🕌) si tu ne veux pas mettre d'image."
                },

                # ==========================================
                # ÉTAPE 3 : LA RÉCOMPENSE
                # ==========================================
                {
                  label: "🎁 FICHIER PDF À GAGNER", 
                  name: "pdf", 
                  widget: "file", 
                  required: false,
                  hint: "Le fichier que l'élève télécharge s'il a 100% de bonnes réponses."
                },

                # ==========================================
                # ÉTAPE 4 : LES QUESTIONS (CHOISIS A OU B)
                # ==========================================
                
                # OPTION A : RAPIDE
                {
                  label: "⚡ OPTION A : Mode IA (Copier-Coller)", 
                  name: "bulk_json", 
                  widget: "text", 
                  required: false,
                  hint: "🟥 Si tu utilises cette case, laisse l'Option B vide. Colle ici le code que ChatGPT te donne."
                },

                # OPTION B : MANUELLE
                {
                  label: "✍️ OPTION B : Mode Manuel (Boutons)", 
                  name: "questions", 
                  widget: "list",
                  collapsed: true,  # C'est ici que je cache la liste pour que ça soit propre !
                  hint: "Clique sur la flèche pour ouvrir. Utilise ça seulement si la case 'Mode IA' est vide.",
                  summary: "❓ {{question}}",
                  fields: [
                    {label: "La Question", name: "question", widget: "string"},
                    {label: "Réponses Possibles (Entrée pour valider)", name: "options", widget: "list"},
                    {label: "La BONNE Réponse (Copie exacte)", name: "answer", widget: "string"},
                    {label: "Explication", name: "explanation", widget: "text", required: false}
                  ]
                }
              ]
            }
