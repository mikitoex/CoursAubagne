backend:
  name: git-gateway
  branch: main

media_folder: "/uploads"
public_folder: "/uploads"
display_url: "https://aubagnecours.netlify.app"
locale: "fr" 

collections:
  - name: "quiz"
    label: "📚 GESTION DES COURS"
    files:
      - name: "data"
        label: "Modifier le contenu"
        file: "quiz.json"
        fields:
          - {
              label: "LES CATÉGORIES (Cliques pour ouvrir)", 
              name: "categories", 
              widget: "list", 
              collapsed: true,   # <--- C'est ça qui crée l'effet "Bloc fermé"
              minimize_collapsed: true,
              summary: "📂 {{title}}", # <--- Le titre affiché sur le bloc
              fields: [
                # ==========================================
                # ÉTAPE 1 : INFOS
                # ==========================================
                {label: "1️⃣ TITRE DU COURS", name: "title", widget: "string"},
                {label: "🔑 IDENTIFIANT (minuscule sans espace)", name: "key", widget: "string"},

                # ==========================================
                # ÉTAPE 2 : VISUEL
                # ==========================================
                {label: "🖼️ IMAGE (Prioritaire)", name: "image", widget: "image", required: false},
                {label: "🙂 EMOJI (Texte)", name: "emoji", widget: "string", required: false},

                # ==========================================
                # ÉTAPE 3 : CADEAU
                # ==========================================
                {label: "🎁 FICHIER PDF", name: "pdf", widget: "file", required: false},

                # ==========================================
                # ÉTAPE 4 : QUESTIONS
                # ==========================================
                {
                  label: "⚡ OPTION A : Mode Copier-Coller IA", 
                  name: "bulk_json", 
                  widget: "text", 
                  required: false,
                  hint: "Colle ici le bloc JSON de ChatGPT."
                },
                {
                  label: "✍️ OPTION B : Mode Manuel", 
                  name: "questions", 
                  widget: "list",
                  collapsed: true, 
                  summary: "{{question}}",
                  fields: [
                    {label: "Question", name: "question", widget: "string"},
                    {label: "Réponses", name: "options", widget: "list"},
                    {label: "Bonne Réponse", name: "answer", widget: "string"},
                    {label: "Explication", name: "explanation", widget: "text", required: false}
                  ]
                }
              ]
            }
