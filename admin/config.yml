backend:
  name: git-gateway
  branch: main

media_folder: "/uploads"
public_folder: "/uploads"
display_url: "https://aubagnecours.netlify.app"
locale: "fr" 

collections:
  - name: "quiz"
    label: "📚 Mes Cours"
    files:
      - name: "data"
        label: "Gérer le Contenu"
        file: "quiz.json"
        fields:
          - {
              label: "Liste des Cours", 
              name: "categories", 
              widget: "list", 
              collapsed: false,
              summary: "📂 {{title}}",
              fields: [
                # --- ÉTAPE 1 : LE LOOK ---
                {label: "--- 1. APPARENCE DU COURS ---", name: "sep1", widget: "hidden", required: false},
                
                {label: "Titre du Cours", name: "title", widget: "string"},
                {label: "Identifiant (un mot simple, ex: priere)", name: "key", widget: "string"},
                
                {label: "Image (Prioritaire)", name: "image", widget: "image", required: false, hint: "L'image s'affiche en premier."},
                {label: "Emoji (Si pas d'image)", name: "emoji", widget: "string", required: false, hint: "S'affiche seulement si la case Image est vide."},

                # --- ÉTAPE 2 : LE CADEAU ---
                {label: "--- 2. LA RÉCOMPENSE ---", name: "sep2", widget: "hidden", required: false},
                
                {label: "Fichier PDF à gagner", name: "pdf", widget: "file", required: false},

                # --- ÉTAPE 3 : LES QUESTIONS ---
                {label: "--- 3. AJOUTER LES QUESTIONS ---", name: "sep3", widget: "hidden", required: false},
                
                # OPTION A : MODE RAPIDE
                {
                  label: "🚀 OPTION A : Coller le code de l'IA ici", 
                  name: "bulk_json", 
                  widget: "text", 
                  required: false,
                  hint: "Si tu utilises cette case, ne touche pas à l'Option B en dessous. Pour modifier une question plus tard, tu devras la modifier ici dans le texte."
                },
                
                # OPTION B : MODE MANUEL
                {
                  label: "✍️ OPTION B : Créer à la main (Boutons)", 
                  name: "questions", 
                  widget: "list",
                  collapsed: true, 
                  summary: "{{question}}",
                  fields: [
                    {label: "Question", name: "question", widget: "string"},
                    {label: "Réponses Possibles", name: "options", widget: "list"},
                    {label: "Bonne Réponse (Copie exacte)", name: "answer", widget: "string"},
                    {label: "Explication", name: "explanation", widget: "text", required: false}
                  ]
                }
              ]
            }
